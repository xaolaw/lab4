
import agh.ics.oop.MoveDirection;
import agh.ics.oop.OptionsParser;
import agh.ics.oop.Vector2d;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import javax.lang.model.type.NullType;

/*public class AnimalTest {
    @Test
    public void walkingtest(){
        Animal turtle = new Animal();
        String[] tablica={"f","right","notToSee","b","trap"};
        OptionsParser test = new OptionsParser();
        MoveDirection[] toPrint = new MoveDirection[1];
        toPrint=test.parse(tablica);
        for (MoveDirection moveDirection : toPrint) {
            if (moveDirection== null)
            {
                break;
            }
            turtle.move(moveDirection);
        }
        Assertions.assertTrue(turtle.isAt(new Vector2d(1,3)));
        Assertions.assertEquals(turtle.toString(),"(1,3) Wschod");
    }
}*/
